# 🐉 DnD Scheduler API

Backend сервис для планирования Dungeons & Dragons сессий. В репозитории оставлена только серверная часть (FastAPI + SQLAlchemy); фронтенд будет развиваться отдельно.

## 🚀 Возможности API
- Управление пользователями и группами
- Создание и применение инвайтов
- Проверка доступности сервера через `/health`

## 🛠 Локальный запуск
1. Запустить базу данных (PostgreSQL). Можно использовать готовый compose:
   ```bash
   docker compose -f .docker/docker-compose.yml up -d
   ```
2. Создать и активировать виртуальное окружение:
   ```bash
   python -m venv .venv
   source .venv/bin/activate
   pip install --upgrade pip
   pip install -r backend/requirements.txt
   ```
3. Создать файл `.env` в корне проекта:
   ```env
   DATABASE_URL="postgresql://app:app@localhost:5432/dnd"
   CORS_ORIGINS=["http://localhost:3000"]
   ```
4. Запустить приложение:
   ```bash
   cd backend
   uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
   ```

Приложение будет доступно по адресу `http://localhost:8000`. Документация Swagger находится на `/docs`.

## 📜 Лицензия
MIT License. Свободно используйте, изменяйте и развивайте.
